using Reflex.Core;
using UnityEngine;

namespace TestSnake.Installers
{
    public class GameSceneInstaller : MonoBehaviour, IInstaller
    {
        public void InstallBindings(ContainerBuilder containerBuilder)
        {
            
        }
    }
}